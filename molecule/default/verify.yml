---
- name: Verify
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Install elastic product
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - metricbeat
        - elasticsearch-oss
