---
- name: Verify
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - metricbeat
        - elasticsearch-oss
      name: Install elastic product
